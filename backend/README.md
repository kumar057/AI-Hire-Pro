# AIHire Pro Backend

FastAPI service foundation for AIHire Pro.

This package intentionally contains infrastructure only: configuration, logging,
database session management, migration wiring, request middleware, JWT utilities,
and a health endpoint.

